# YouTube hands ffmpeg URLs that need matching client headers; the android
# client returns plain progressive URLs mpv can fetch on its own.
# If playback breaks after a yt-dlp update, try another client here.
ytdl-raw-options=extractor-args="youtube:player_client=android"
