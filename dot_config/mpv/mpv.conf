# nothing to work around here at the moment — yt-dlp 2026.08.19 hands mpv
# streams it can fetch on its own. If playback breaks after an update, try
# ytdl-raw-options=extractor-args="youtube:player_client=android"
